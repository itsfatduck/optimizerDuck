<UserControl
    x:Class="optimizerDuck.UI.Controls.LoadingStateOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    x:Name="LoadingRoot">
    <Grid Visibility="{Binding IsLoading, ElementName=LoadingRoot, Converter={StaticResource BooleanToVisibilityConverter}}">
        <Border
            Background="{DynamicResource ApplicationBackgroundBrush}"
            Opacity="0.85"
            Visibility="{Binding DimBackground, ElementName=LoadingRoot, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <ui:ProgressRing
                Width="48"
                Height="48"
                Margin="0,0,0,16"
                IsIndeterminate="True" />
            <ui:TextBlock
                HorizontalAlignment="Center"
                FontTypography="BodyStrong"
                Text="{Binding Title, ElementName=LoadingRoot}"
                Visibility="{Binding Title, ElementName=LoadingRoot, Converter={StaticResource NotNullToVisibilityConverter}}" />
            <ui:TextBlock
                Margin="0,4,0,0"
                HorizontalAlignment="Center"
                FontTypography="Caption"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                Text="{Binding Subtitle, ElementName=LoadingRoot}"
                Visibility="{Binding Subtitle, ElementName=LoadingRoot, Converter={StaticResource NotNullToVisibilityConverter}}" />
        </StackPanel>
    </Grid>
</UserControl>
