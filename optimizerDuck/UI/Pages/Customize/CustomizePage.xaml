<Page
    x:Class="optimizerDuck.UI.Pages.CustomizePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:optimizerDuck.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ext="clr-namespace:optimizerDuck.Common.Extensions"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pages="clr-namespace:optimizerDuck.UI.Pages"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:optimizerDuck.UI.ViewModels.Pages"
    Title="Customize"
    d:DataContext="{d:DesignInstance pages:CustomizePage}"
    d:DesignHeight="650"
    d:DesignWidth="800"
    ui:Design.Background="Black"
    ui:Design.Foreground="White"
    Style="{StaticResource PageRootStyle}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Style="{StaticResource PageHeaderStyle}">
            <ui:TextBlock Style="{StaticResource PageTitleStyle}" Text="{ext:Loc Customize.Title}" />
            <ui:TextBlock Style="{StaticResource PageDescriptionStyle}" Text="{ext:Loc Customize.Description}" />
        </StackPanel>

        <ui:VirtualizingItemsControl
            Grid.Row="1"
            Margin="0"
            ItemsSource="{Binding ViewModel.Categories}">
            <ui:VirtualizingItemsControl.Template>
                <ControlTemplate TargetType="ItemsControl">
                    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                        <ItemsPresenter />
                    </ScrollViewer>
                </ControlTemplate>
            </ui:VirtualizingItemsControl.Template>

            <ui:VirtualizingItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel />
                </ItemsPanelTemplate>
            </ui:VirtualizingItemsControl.ItemsPanel>

            <ui:VirtualizingItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Margin" Value="0,0,12,12" />
                    <Setter Property="HorizontalAlignment" Value="Stretch" />
                </Style>
            </ui:VirtualizingItemsControl.ItemContainerStyle>

            <ui:VirtualizingItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:CustomizeCategoryItemViewModel}">
                    <ui:CardAction
                        Padding="20,18"
                        Command="{Binding DataContext.ViewModel.NavigateToCategoryCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                        CommandParameter="{Binding}"
                        Cursor="Hand">
                        <Grid ColumnDefinitions="Auto,Auto">
                            <ui:SymbolIcon
                                Margin="0,0,15,0"
                                FontSize="30"
                                Foreground="{DynamicResource AccentYellowBrush}"
                                Symbol="{Binding Icon}" />

                            <StackPanel Grid.Column="1">
                                <ui:TextBlock
                                    Grid.Row="0"
                                    VerticalAlignment="Center"
                                    FontTypography="Subtitle"
                                    Text="{Binding Name}"
                                    TextWrapping="Wrap" />
                                <ui:TextBlock
                                    Grid.Row="1"
                                    Appearance="Secondary"
                                    FontTypography="Body"
                                    Text="{Binding Description}"
                                    TextWrapping="Wrap" />
                            </StackPanel>
                        </Grid>
                    </ui:CardAction>
                </DataTemplate>
            </ui:VirtualizingItemsControl.ItemTemplate>
        </ui:VirtualizingItemsControl>

        <controls:LoadingStateOverlay
            Grid.Row="1"
            IsLoading="{Binding ViewModel.IsLoading}"
            Title="{ext:Loc Customize.Loading}" />
    </Grid>
</Page>
