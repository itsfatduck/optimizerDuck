<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <!--  Custom Left Navigation Item Template with animation  -->
    <ControlTemplate x:Key="AdaptiveLeftCompactNavigationViewItemTemplate" TargetType="{x:Type ui:NavigationViewItem}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Border
                x:Name="MainBorder"
                Grid.Row="0"
                MinWidth="40"
                MinHeight="40"
                Margin="0"
                Padding="0"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{DynamicResource NavigationViewItemThickness}"
                CornerRadius="{TemplateBinding Border.CornerRadius}">
                <Border.Style>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="Background" Value="{DynamicResource NavigationViewItemBackground}" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource NavigationViewItemBackgroundPointerOver}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid Background="Transparent">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="40" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0">
                        <ContentPresenter
                            x:Name="IconContentPresenter"
                            Margin="-1,0,0,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Content="{TemplateBinding Icon}"
                            Focusable="False"
                            TextElement.FontSize="16"
                            TextElement.Foreground="{DynamicResource NavigationViewItemForeground}" />
                    </Grid>

                    <Rectangle
                        x:Name="ActiveRectangle"
                        Grid.Column="0"
                        Width="3"
                        Height="16"
                        Margin="0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Fill="{DynamicResource NavigationViewSelectionIndicatorForeground}"
                        Opacity="0.0"
                        RadiusX="2"
                        RadiusY="2" />

                    <ContentPresenter
                        x:Name="ElementContentPresenter"
                        Grid.Column="1"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Content="{TemplateBinding Content}"
                        ContentTemplate="{TemplateBinding ContentTemplate}"
                        RecognizesAccessKey="True"
                        TextElement.FontSize="14"
                        TextElement.Foreground="{TemplateBinding Foreground}" />

                    <ContentPresenter
                        x:Name="PART_InfoBadge"
                        Grid.Column="2"
                        Margin="0"
                        Content="{TemplateBinding InfoBadge}" />

                    <Grid
                        x:Name="PART_ChevronGrid"
                        Grid.Column="3"
                        Width="0"
                        Visibility="Collapsed">
                        <ui:SymbolIcon
                            x:Name="ChevronIcon"
                            Margin="0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontSize="{StaticResource NavigationViewItemChevronSize}"
                            Foreground="{DynamicResource NavigationViewItemForeground}"
                            RenderTransformOrigin="0.5, 0.5"
                            Symbol="ChevronDown24">
                            <ui:SymbolIcon.RenderTransform>
                                <RotateTransform Angle="0" />
                            </ui:SymbolIcon.RenderTransform>
                        </ui:SymbolIcon>
                    </Grid>
                </Grid>
            </Border>
            <ItemsControl
                x:Name="MenuItemsPresenter"
                Grid.Row="1"
                AutomationProperties.AutomationId="NavigationItemItems"
                Focusable="False"
                ItemsSource="{TemplateBinding MenuItems}"
                KeyboardNavigation.DirectionalNavigation="Contained"
                Opacity="0.0"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                ScrollViewer.VerticalScrollBarVisibility="Disabled"
                Visibility="Collapsed">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Margin="0" IsItemsHost="True" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.Resources>
                    <Style BasedOn="{StaticResource NavigationViewItemDefaultStyle}" TargetType="{x:Type ui:NavigationViewItem}">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ui:NavigationViewItem}">
                                    <Border
                                        x:Name="MainBorder"
                                        Height="40"
                                        HorizontalAlignment="Stretch"
                                        Background="Transparent"
                                        BorderThickness="{DynamicResource NavigationViewItemThickness}"
                                        CornerRadius="4">
                                        <Grid Margin="30,0,0,0" HorizontalAlignment="Stretch">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" MinWidth="180" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <Rectangle
                                                x:Name="ActiveRectangle"
                                                Grid.Column="0"
                                                Width="3"
                                                Height="16"
                                                Margin="0"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Center"
                                                Fill="{DynamicResource NavigationViewSelectionIndicatorForeground}"
                                                Opacity="0.0"
                                                RadiusX="2"
                                                RadiusY="2" />

                                            <ContentPresenter
                                                x:Name="IconContentPresenter"
                                                Grid.Column="1"
                                                Margin="6,0,3,0"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                Content="{TemplateBinding Icon}"
                                                Focusable="False"
                                                TextElement.FontSize="18"
                                                TextElement.Foreground="{DynamicResource NavigationViewItemForeground}"
                                                Visibility="{TemplateBinding Icon,
                                                                             Converter={StaticResource NullToVisibilityConverter}}" />
                                            <ContentPresenter
                                                x:Name="ElementContentPresenter"
                                                Grid.Column="2"
                                                Margin="10,0,0,0"
                                                HorizontalAlignment="Stretch"
                                                VerticalAlignment="Center"
                                                Content="{TemplateBinding Content}"
                                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                                RecognizesAccessKey="True"
                                                TextElement.FontSize="14"
                                                TextElement.Foreground="{TemplateBinding Foreground}" />

                                            <ContentPresenter Grid.Column="3" Content="{TemplateBinding InfoBadge}" />
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsActive" Value="True">
                                            <Trigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation
                                                            Storyboard.TargetName="ActiveRectangle"
                                                            Storyboard.TargetProperty="Opacity"
                                                            From="0"
                                                            To="1"
                                                            Duration="0:0:0.2">
                                                            <DoubleAnimation.EasingFunction>
                                                                <CircleEase EasingMode="EaseOut" />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                        <DoubleAnimation
                                                            Storyboard.TargetName="ActiveRectangle"
                                                            Storyboard.TargetProperty="Height"
                                                            From="4"
                                                            To="16"
                                                            Duration="0:0:0.25">
                                                            <DoubleAnimation.EasingFunction>
                                                                <CircleEase EasingMode="EaseOut" />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation
                                                            Storyboard.TargetName="ActiveRectangle"
                                                            Storyboard.TargetProperty="Opacity"
                                                            From="1"
                                                            To="0"
                                                            Duration="0:0:0.2">
                                                            <DoubleAnimation.EasingFunction>
                                                                <CircleEase EasingMode="EaseOut" />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                        <DoubleAnimation
                                                            Storyboard.TargetName="ActiveRectangle"
                                                            Storyboard.TargetProperty="Height"
                                                            From="16"
                                                            To="4"
                                                            Duration="0:0:0.25">
                                                            <DoubleAnimation.EasingFunction>
                                                                <CircleEase EasingMode="EaseOut" />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.ExitActions>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="MainBorder" Property="Background" Value="{DynamicResource NavigationViewItemBackgroundPointerOver}" />
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="MainBorder" Property="Background" Value="{DynamicResource NavigationViewItemBackgroundPressed}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ItemsControl.Resources>
            </ItemsControl>
        </Grid>
        <ControlTemplate.Triggers>

            <Trigger Property="IsPaneOpen" Value="True">
                <Setter TargetName="PART_InfoBadge" Property="Grid.Column" Value="2" />
            </Trigger>
            <Trigger Property="IsPaneOpen" Value="False">
                <Setter TargetName="PART_InfoBadge" Property="Grid.Column" Value="0" />
                <Setter TargetName="PART_InfoBadge" Property="VerticalAlignment" Value="Top" />
                <Setter TargetName="PART_InfoBadge" Property="HorizontalAlignment" Value="Right" />
            </Trigger>

            <Trigger Property="InfoBadge" Value="{x:Null}">
                <Setter TargetName="PART_InfoBadge" Property="Margin" Value="0" />
                <Setter TargetName="PART_InfoBadge" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="HasMenuItems" Value="True">
                <Setter TargetName="PART_ChevronGrid" Property="Visibility" Value="Visible" />
                <Setter TargetName="PART_ChevronGrid" Property="Width" Value="40" />
            </Trigger>
            <Trigger Property="IsExpanded" Value="True">
                <Setter TargetName="MenuItemsPresenter" Property="Visibility" Value="Visible" />
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="MenuItemsPresenter"
                                Storyboard.TargetProperty="(ItemsControl.Opacity)"
                                From="0.0"
                                To="1.0"
                                Duration="00:00:00.167" />
                            <DoubleAnimation
                                Storyboard.TargetName="ChevronIcon"
                                Storyboard.TargetProperty="(Control.RenderTransform).(RotateTransform.Angle)"
                                To="-180"
                                Duration="00:00:00.167" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="MenuItemsPresenter"
                                Storyboard.TargetProperty="(ItemsControl.Opacity)"
                                From="1.0"
                                To="0.0"
                                Duration="00:00:00.167" />
                            <DoubleAnimation
                                Storyboard.TargetName="ChevronIcon"
                                Storyboard.TargetProperty="(Control.RenderTransform).(RotateTransform.Angle)"
                                To="0"
                                Duration="00:00:00.167" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>

            <Trigger Property="IsActive" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="ActiveRectangle"
                                Storyboard.TargetProperty="Opacity"
                                From="0"
                                To="1"
                                Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <CircleEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation
                                Storyboard.TargetName="ActiveRectangle"
                                Storyboard.TargetProperty="Height"
                                From="4"
                                To="16"
                                Duration="0:0:0.25">
                                <DoubleAnimation.EasingFunction>
                                    <CircleEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="ActiveRectangle"
                                Storyboard.TargetProperty="Opacity"
                                From="1"
                                To="0"
                                Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <CircleEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation
                                Storyboard.TargetName="ActiveRectangle"
                                Storyboard.TargetProperty="Height"
                                From="16"
                                To="4"
                                Duration="0:0:0.25">
                                <DoubleAnimation.EasingFunction>
                                    <CircleEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
            <Trigger Property="Icon" Value="{x:Null}">
                <Setter TargetName="IconContentPresenter" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="IsActive" Value="False" />
                </MultiTrigger.Conditions>
                <MultiTrigger.Setters>
                    <Setter TargetName="IconContentPresenter" Property="TextElement.Foreground" Value="{DynamicResource NavigationViewItemForegroundPointerOver}" />
                    <Setter TargetName="ElementContentPresenter" Property="TextElement.Foreground" Value="{DynamicResource NavigationViewItemForegroundPointerOver}" />
                </MultiTrigger.Setters>
            </MultiTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!--  Custom Top Navigation Item Template with active indicator fix and animation  -->
    <ControlTemplate x:Key="ScrollableTopCompactNavigationViewItemTemplate" TargetType="{x:Type ui:NavigationViewItem}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Border
                x:Name="MainBorder"
                Padding="0,0,0,4"
                Background="Transparent">
                <Grid>
                    <Grid
                        Margin="8,8,8,4"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ContentPresenter
                            x:Name="IconContentPresenter"
                            Grid.Column="0"
                            Margin="0,0,6,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Content="{TemplateBinding Icon}"
                            TextElement.FontSize="16"
                            TextElement.Foreground="{DynamicResource NavigationViewItemForeground}" />

                        <ContentPresenter
                            x:Name="ElementContentPresenter"
                            Grid.Column="1"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            RecognizesAccessKey="True"
                            TextElement.FontSize="14"
                            TextElement.Foreground="{TemplateBinding Foreground}" />
                        <Grid
                            x:Name="PART_ChevronGrid"
                            Grid.Column="2"
                            Width="0"
                            Visibility="Collapsed" />

                        <ContentPresenter
                            x:Name="PART_InfoBadge"
                            Grid.Column="2"
                            Margin="2"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Content="{TemplateBinding InfoBadge}" />
                    </Grid>

                    <Rectangle
                        x:Name="ActiveRectangle"
                        Width="16"
                        Height="3"
                        Margin="0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Bottom"
                        Fill="{DynamicResource NavigationViewSelectionIndicatorForeground}"
                        Opacity="0.0"
                        RadiusX="2"
                        RadiusY="2" />
                </Grid>
            </Border>
            <Popup
                x:Name="SubMenuPopup"
                Grid.Row="1"
                AllowsTransparency="True"
                Focusable="False"
                HorizontalOffset="-12"
                IsOpen="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                Placement="Bottom"
                PlacementTarget="{Binding ElementName=MainBorder}"
                PopupAnimation="None"
                StaysOpen="False"
                VerticalOffset="1">

                <Border
                    x:Name="SubMenuBorder"
                    Margin="12,0,12,18"
                    Padding="0,3,0,3"
                    Background="{DynamicResource FlyoutBackground}"
                    BorderBrush="{DynamicResource FlyoutBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    SnapsToDevicePixels="True">
                    <Border.RenderTransform>
                        <TranslateTransform />
                    </Border.RenderTransform>
                    <Border.Effect>
                        <DropShadowEffect
                            BlurRadius="20"
                            Direction="270"
                            Opacity="0.25"
                            ShadowDepth="6" />
                    </Border.Effect>

                    <ItemsControl
                        x:Name="SubMenuItemsPresenter"
                        Focusable="False"
                        ItemsSource="{TemplateBinding MenuItems}"
                        KeyboardNavigation.DirectionalNavigation="Contained"
                        Opacity="0.0"
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                        ScrollViewer.VerticalScrollBarVisibility="Disabled">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Margin="0" IsItemsHost="True" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.Resources>
                            <Style BasedOn="{StaticResource NavigationViewItemDefaultStyle}" TargetType="{x:Type ui:NavigationViewItem}">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type ui:NavigationViewItem}">
                                            <Border
                                                x:Name="MainBorder"
                                                Height="25"
                                                Margin="0"
                                                Padding="0"
                                                HorizontalAlignment="Stretch"
                                                BorderThickness="1"
                                                CornerRadius="4">
                                                <Grid Margin="0" HorizontalAlignment="Stretch">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="3" />
                                                        <ColumnDefinition Width="15" />
                                                        <ColumnDefinition Width="*" MinWidth="120" />
                                                    </Grid.ColumnDefinitions>
                                                    <Rectangle
                                                        x:Name="ActiveRectangle"
                                                        Grid.Column="0"
                                                        Width="3"
                                                        Height="16"
                                                        Margin="0"
                                                        HorizontalAlignment="Left"
                                                        VerticalAlignment="Center"
                                                        Fill="{DynamicResource NavigationViewSelectionIndicatorForeground}"
                                                        Opacity="0.0"
                                                        RadiusX="2"
                                                        RadiusY="2" />

                                                    <ContentPresenter
                                                        x:Name="IconContentPresenter"
                                                        Grid.Column="1"
                                                        Margin="0,0,0,0"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"
                                                        Content="{TemplateBinding Icon}"
                                                        Focusable="False"
                                                        TextElement.FontSize="16"
                                                        TextElement.Foreground="{DynamicResource NavigationViewItemForeground}" />
                                                    <ContentPresenter
                                                        x:Name="ElementContentPresenter"
                                                        Grid.Column="2"
                                                        Margin="5,0,0,0"
                                                        HorizontalAlignment="Stretch"
                                                        VerticalAlignment="Center"
                                                        Content="{TemplateBinding Content}"
                                                        ContentTemplate="{TemplateBinding ContentTemplate}"
                                                        RecognizesAccessKey="True"
                                                        TextElement.FontSize="14"
                                                        TextElement.Foreground="{DynamicResource NavigationViewItemForeground}" />
                                                </Grid>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsActive" Value="True">
                                                    <Trigger.EnterActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <DoubleAnimation
                                                                    Storyboard.TargetName="ActiveRectangle"
                                                                    Storyboard.TargetProperty="Opacity"
                                                                    From="0"
                                                                    To="1"
                                                                    Duration="0:0:0.2">
                                                                    <DoubleAnimation.EasingFunction>
                                                                        <CircleEase EasingMode="EaseOut" />
                                                                    </DoubleAnimation.EasingFunction>
                                                                </DoubleAnimation>
                                                                <DoubleAnimation
                                                                    Storyboard.TargetName="ActiveRectangle"
                                                                    Storyboard.TargetProperty="Height"
                                                                    From="4"
                                                                    To="16"
                                                                    Duration="0:0:0.25">
                                                                    <DoubleAnimation.EasingFunction>
                                                                        <CircleEase EasingMode="EaseOut" />
                                                                    </DoubleAnimation.EasingFunction>
                                                                </DoubleAnimation>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </Trigger.EnterActions>
                                                    <Trigger.ExitActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <DoubleAnimation
                                                                    Storyboard.TargetName="ActiveRectangle"
                                                                    Storyboard.TargetProperty="Opacity"
                                                                    From="1"
                                                                    To="0"
                                                                    Duration="0:0:0.2">
                                                                    <DoubleAnimation.EasingFunction>
                                                                        <CircleEase EasingMode="EaseOut" />
                                                                    </DoubleAnimation.EasingFunction>
                                                                </DoubleAnimation>
                                                                <DoubleAnimation
                                                                    Storyboard.TargetName="ActiveRectangle"
                                                                    Storyboard.TargetProperty="Height"
                                                                    From="16"
                                                                    To="4"
                                                                    Duration="0:0:0.25">
                                                                    <DoubleAnimation.EasingFunction>
                                                                        <CircleEase EasingMode="EaseOut" />
                                                                    </DoubleAnimation.EasingFunction>
                                                                </DoubleAnimation>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </Trigger.ExitActions>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="MainBorder" Property="Background" Value="{DynamicResource NavigationViewItemBackgroundPointerOver}" />
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="MainBorder" Property="Background" Value="{DynamicResource NavigationViewItemBackgroundPressed}" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ItemsControl.Resources>
                    </ItemsControl>
                </Border>
            </Popup>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="HasMenuItems" Value="True">
                <Setter TargetName="PART_ChevronGrid" Property="Visibility" Value="Visible" />
                <Setter TargetName="PART_ChevronGrid" Property="Width" Value="20" />
            </Trigger>
            <Trigger Property="IsExpanded" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="SubMenuItemsPresenter"
                                Storyboard.TargetProperty="(UIElement.Opacity)"
                                From="0.0"
                                To="1.0"
                                Duration="0:0:0.167" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="SubMenuItemsPresenter"
                                Storyboard.TargetProperty="(UIElement.Opacity)"
                                From="1.0"
                                To="0.0"
                                Duration="0:0:0.167" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
            <Trigger Property="IsActive" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="ActiveRectangle"
                                Storyboard.TargetProperty="Opacity"
                                From="0"
                                To="1"
                                Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <CircleEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="ActiveRectangle"
                                Storyboard.TargetProperty="Opacity"
                                From="1"
                                To="0"
                                Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <CircleEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
            <Trigger Property="Icon" Value="{x:Null}">
                <Setter TargetName="IconContentPresenter" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="Content" Value="{x:Null}">
                <Setter TargetName="IconContentPresenter" Property="Margin" Value="0" />
                <Setter TargetName="ElementContentPresenter" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="Content" Value="">
                <Setter TargetName="IconContentPresenter" Property="Margin" Value="0" />
                <Setter TargetName="ElementContentPresenter" Property="Visibility" Value="Collapsed" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!--  Custom Left Navigation Template with auto-width pane  -->
    <ControlTemplate x:Key="AdaptiveLeftNavigationViewTemplate" TargetType="{x:Type ui:NavigationView}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid
                x:Name="PaneGrid"
                Grid.Column="0"
                Width="Auto"
                Margin="4,0,4,0">
                <Border x:Name="PaneBorder">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <ui:Button
                                x:Name="PART_BackButton"
                                Grid.Row="0"
                                Margin="0,5,0,5"
                                HorizontalAlignment="Left"
                                AutomationProperties.AutomationId="NavigationBackButton"
                                IsEnabled="{TemplateBinding IsBackEnabled}"
                                Style="{StaticResource BasePaneButtonStyle}"
                                Visibility="{TemplateBinding IsBackButtonVisible,
                                                             Converter={StaticResource BackButtonVisibilityToVisibilityConverter}}">
                                <ui:Button.Icon>
                                    <ui:SymbolIcon Symbol="ArrowLeft48" />
                                </ui:Button.Icon>
                            </ui:Button>

                            <ui:Button
                                x:Name="PART_ToggleButton"
                                Grid.Row="1"
                                Margin="0,0,0,5"
                                AutomationProperties.AutomationId="NavigationToggleButton"
                                Style="{StaticResource BasePaneButtonStyle}"
                                Visibility="{TemplateBinding IsPaneToggleVisible,
                                                             Converter={StaticResource BoolToVisibilityConverter}}">
                                <ui:Button.Icon>
                                    <ui:SymbolIcon Symbol="LineHorizontal320" />
                                </ui:Button.Icon>
                                <ui:Button.Content>
                                    <TextBlock FontWeight="Medium" Text="{TemplateBinding PaneTitle}" />
                                </ui:Button.Content>
                            </ui:Button>

                            <ContentPresenter
                                Grid.Row="2"
                                Margin="0"
                                Content="{TemplateBinding PaneHeader}" />

                            <ContentPresenter
                                x:Name="AutoSuggestBoxContentPresenter"
                                Grid.Row="3"
                                Margin="0,0,0,6"
                                Content="{TemplateBinding AutoSuggestBox}" />

                            <ui:Button
                                x:Name="PART_AutoSuggestBoxSymbolButton"
                                Grid.Row="3"
                                Margin="0,0,0,6"
                                AutomationProperties.AutomationId="NavigationAutoSuggestBoxButton"
                                Icon="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=AutoSuggestBox.Icon}"
                                Style="{StaticResource BasePaneButtonStyle}"
                                Visibility="Hidden" />
                        </Grid>

                        <Border
                            x:Name="PART_TopSeparator"
                            Grid.Row="1"
                            Height="1"
                            Margin="0,4,0,4"
                            Background="{DynamicResource LeftNavigationViewSeparatorBrush}" />

                        <ui:DynamicScrollViewer
                            x:Name="PART_ScrollViewer"
                            Grid.Row="2"
                            Margin="0,0,-4,0"
                            Padding="0,0,4,0"
                            CanContentScroll="True"
                            HorizontalScrollBarVisibility="Disabled"
                            VerticalScrollBarVisibility="Auto">
                            <ItemsControl
                                x:Name="PART_MenuItemsItemsControl"
                                AutomationProperties.AutomationId="NavigationItems"
                                Focusable="False"
                                KeyboardNavigation.DirectionalNavigation="Contained"
                                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                ScrollViewer.VerticalScrollBarVisibility="Disabled">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel IsItemsHost="True" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </ui:DynamicScrollViewer>

                        <Border
                            x:Name="PART_FooterSeparator"
                            Grid.Row="3"
                            Height="1"
                            Margin="0,4,0,4"
                            Background="{DynamicResource LeftNavigationViewSeparatorBrush}" />

                        <Grid Grid.Row="4">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <ItemsControl
                                x:Name="PART_FooterMenuItemsItemsControl"
                                Grid.Row="0"
                                Margin="0,0,0,4"
                                AutomationProperties.AutomationId="NavigationFooterItems"
                                Focusable="False"
                                KeyboardNavigation.DirectionalNavigation="Contained"
                                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                ScrollViewer.VerticalScrollBarVisibility="Disabled">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Margin="0" IsItemsHost="True" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>

                            <ContentPresenter
                                Grid.Row="1"
                                Margin="0"
                                Content="{TemplateBinding PaneFooter}" />
                        </Grid>
                    </Grid>
                </Border>
            </Grid>

            <Border
                Grid.Column="1"
                Margin="{TemplateBinding FrameMargin}"
                Background="{DynamicResource NavigationViewContentBackground}"
                BorderBrush="{DynamicResource NavigationViewContentGridBorderBrush}"
                BorderThickness="1,1,0,0"
                CornerRadius="8,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <ContentPresenter
                        Grid.Row="0"
                        Margin="0"
                        Content="{TemplateBinding Header}"
                        Visibility="{TemplateBinding HeaderVisibility}" />

                    <ui:NavigationViewContentPresenter
                        x:Name="PART_NavigationViewContentPresenter"
                        Grid.Row="1"
                        Padding="{TemplateBinding Padding}"
                        Transition="{TemplateBinding Transition}"
                        TransitionDuration="{TemplateBinding TransitionDuration}">
                        <ui:NavigationViewContentPresenter.Template>
                            <ControlTemplate TargetType="{x:Type ui:NavigationViewContentPresenter}">
                                <ContentPresenter x:Name="PART_FrameCP" Margin="{TemplateBinding Padding}" />
                            </ControlTemplate>
                        </ui:NavigationViewContentPresenter.Template>
                    </ui:NavigationViewContentPresenter>

                    <ContentPresenter
                        Grid.Row="0"
                        Grid.RowSpan="2"
                        Margin="0"
                        Content="{TemplateBinding ContentOverlay}" />
                </Grid>
            </Border>

            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup Name="PaneStates">
                    <VisualState Name="PaneOpen">
                        <Storyboard>
                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="AutoSuggestBoxContentPresenter" Storyboard.TargetProperty="Visibility">
                                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}" />
                            </ObjectAnimationUsingKeyFrames>
                            <DoubleAnimation
                                AccelerationRatio="0.4"
                                Storyboard.TargetName="AutoSuggestBoxContentPresenter"
                                Storyboard.TargetProperty="Opacity"
                                From="0"
                                To="1"
                                Duration="0:0:.16" />
                        </Storyboard>
                    </VisualState>
                    <VisualState Name="PaneCompact">
                        <Storyboard>
                            <DoubleAnimation
                                AccelerationRatio="0.4"
                                Storyboard.TargetName="AutoSuggestBoxContentPresenter"
                                Storyboard.TargetProperty="Opacity"
                                From="1"
                                To="0"
                                Duration="0:0:.16" />
                            <DoubleAnimation
                                AccelerationRatio="0.4"
                                Storyboard.TargetName="PART_AutoSuggestBoxSymbolButton"
                                Storyboard.TargetProperty="Opacity"
                                From="0"
                                To="1"
                                Duration="0:0:.16" />
                            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="PaneGrid" Storyboard.TargetProperty="Width">
                                <DiscreteDoubleKeyFrame KeyTime="0:0:0" Value="40" />
                            </DoubleAnimationUsingKeyFrames>
                            <ObjectAnimationUsingKeyFrames
                                BeginTime="0:0:0.16"
                                Storyboard.TargetName="AutoSuggestBoxContentPresenter"
                                Storyboard.TargetProperty="Visibility">
                                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Hidden}" />
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
        </Grid>

        <ControlTemplate.Triggers>
            <Trigger Property="IsPaneOpen" Value="False">
                <Setter TargetName="PART_AutoSuggestBoxSymbolButton" Property="Visibility" Value="Visible" />
                <Setter TargetName="PART_ToggleButton" Property="Content" Value="{x:Null}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsBackButtonVisible" Value="Collapsed" />
                    <Condition Property="IsPaneToggleVisible" Value="False" />
                    <Condition Property="AutoSuggestBox" Value="{x:Null}" />
                    <Condition Property="PaneHeader" Value="{x:Null}" />
                </MultiTrigger.Conditions>
                <Setter TargetName="PART_TopSeparator" Property="Visibility" Value="Collapsed" />
            </MultiTrigger>
            <Trigger Property="AutoSuggestBox" Value="{x:Null}">
                <Setter TargetName="PART_AutoSuggestBoxSymbolButton" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="AutoSuggestBoxContentPresenter" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="PaneTitle" Value="{x:Null}">
                <Setter TargetName="PART_ToggleButton" Property="Content" Value="{x:Null}" />
                <Setter TargetName="PART_ToggleButton" Property="HorizontalAlignment" Value="Left" />
            </Trigger>
            <Trigger Property="IsTopSeparatorVisible" Value="False">
                <Setter TargetName="PART_TopSeparator" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsFooterSeparatorVisible" Value="False">
                <Setter TargetName="PART_FooterSeparator" Property="Visibility" Value="Collapsed" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!--  Scrollable Top Navigation Template  -->
    <ControlTemplate x:Key="ScrollableTopNavigationViewTemplate" TargetType="{x:Type ui:NavigationView}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid x:Name="PaneGrid" Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <ui:Button
                    x:Name="PART_BackButton"
                    Grid.Column="0"
                    IsEnabled="{TemplateBinding IsBackEnabled}"
                    Style="{StaticResource BasePaneButtonStyle}"
                    Visibility="{TemplateBinding IsBackButtonVisible,
                                                 Converter={StaticResource BackButtonVisibilityToVisibilityConverter}}">
                    <ui:Button.Icon>
                        <ui:SymbolIcon Symbol="ArrowLeft48" />
                    </ui:Button.Icon>
                </ui:Button>

                <ContentPresenter
                    Grid.Column="1"
                    Margin="0"
                    Content="{TemplateBinding PaneHeader}" />

                <ScrollViewer
                    Grid.Column="2"
                    CanContentScroll="False"
                    HorizontalScrollBarVisibility="Auto"
                    VerticalScrollBarVisibility="Disabled">
                    <ItemsControl
                        x:Name="PART_MenuItemsItemsControl"
                        Focusable="False"
                        KeyboardNavigation.DirectionalNavigation="Contained">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel
                                    Margin="0"
                                    IsItemsHost="True"
                                    Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ScrollViewer>

                <ContentPresenter Grid.Column="3" Content="{TemplateBinding AutoSuggestBox}" />

                <ContentPresenter
                    Grid.Column="4"
                    Margin="0"
                    Content="{TemplateBinding PaneFooter}" />

                <ScrollViewer
                    Grid.Column="5"
                    CanContentScroll="False"
                    HorizontalScrollBarVisibility="Auto"
                    VerticalScrollBarVisibility="Disabled">
                    <ItemsControl
                        x:Name="PART_FooterMenuItemsItemsControl"
                        Focusable="False"
                        KeyboardNavigation.DirectionalNavigation="Contained">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel
                                    Margin="0"
                                    IsItemsHost="True"
                                    Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <ContentPresenter
                    Grid.Row="0"
                    Margin="0"
                    Content="{TemplateBinding Header}"
                    Visibility="{TemplateBinding HeaderVisibility}" />

                <ui:NavigationViewContentPresenter
                    x:Name="PART_NavigationViewContentPresenter"
                    Grid.Row="1"
                    Padding="{TemplateBinding Padding}"
                    Transition="{TemplateBinding Transition}"
                    TransitionDuration="{TemplateBinding TransitionDuration}">
                    <ui:NavigationViewContentPresenter.Template>
                        <ControlTemplate TargetType="{x:Type ui:NavigationViewContentPresenter}">
                            <ContentPresenter x:Name="PART_FrameCP" Margin="{TemplateBinding Padding}" />
                        </ControlTemplate>
                    </ui:NavigationViewContentPresenter.Template>
                </ui:NavigationViewContentPresenter>

                <ContentPresenter
                    Grid.Row="0"
                    Grid.RowSpan="2"
                    Margin="0"
                    Content="{TemplateBinding ContentOverlay}" />
            </Grid>
        </Grid>
    </ControlTemplate>

    <!--  Adaptive pane style for MainWindow NavigationView  -->
    <Style
        x:Key="AdaptivePaneNavigationViewStyle"
        BasedOn="{StaticResource DefaultNavigationViewStyle}"
        TargetType="{x:Type ui:NavigationView}">
        <Style.Triggers>
            <Trigger Property="PaneDisplayMode" Value="Left">
                <Setter Property="Template" Value="{StaticResource AdaptiveLeftNavigationViewTemplate}" />
                <Setter Property="ItemTemplate" Value="{StaticResource AdaptiveLeftCompactNavigationViewItemTemplate}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--  Scrollable top style for NavigationView  -->
    <Style
        x:Key="ScrollableTopNavigationViewStyle"
        BasedOn="{StaticResource DefaultNavigationViewStyle}"
        TargetType="{x:Type ui:NavigationView}">
        <Style.Triggers>
            <Trigger Property="PaneDisplayMode" Value="Top">
                <Setter Property="Template" Value="{StaticResource ScrollableTopNavigationViewTemplate}" />
                <Setter Property="ItemTemplate" Value="{StaticResource ScrollableTopCompactNavigationViewItemTemplate}" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
