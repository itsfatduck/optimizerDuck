<UserControl
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d"
    x:Class="optimizerDuck.UI.Dialogs.ProcessingDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:converters="clr-namespace:optimizerDuck.Common.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:extensions="clr-namespace:optimizerDuck.Common.Extensions"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
>
    <UserControl.Resources>
        <converters:GreaterThanZeroToVisibilityConverter x:Key="GreaterThanZeroToVisibilityConverter" />
    </UserControl.Resources>

    <StackPanel MinWidth="420">
        <ui:TextBlock
            HorizontalAlignment="Center"
            Margin="0,0,0,12"
            FontTypography="BodyStrong"
            Text="{Binding Message}"
            TextAlignment="Center"
            TextWrapping="Wrap" />

        <ProgressBar
            Height="6"
            IsIndeterminate="{Binding IsIndeterminate}"
            Maximum="{Binding Total}"
            Minimum="0"
            Value="{Binding Value}"
            Visibility="{Binding Total, Converter={StaticResource GreaterThanZeroToVisibilityConverter}}"
        />

        <ui:TextBlock
            FontTypography="Caption"
            Foreground="{DynamicResource SystemFillColorCriticalBrush}"
            HorizontalAlignment="Center"
            Margin="0,16,0,0"
            Text="{extensions:Loc ProcessingOptimizationDialog.Warning.DontCloseAppOrRestart}"
            TextAlignment="Center"
            TextWrapping="Wrap"
        />
    </StackPanel>
</UserControl>
