<UserControl
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d"
    x:Class="optimizerDuck.UI.Views.Dialogs.ProcessingOptimizationDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:extensions="clr-namespace:optimizerDuck.Common.Extensions"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock
            HorizontalAlignment="Center"
            Margin="0,0,0,5"
            Text="{Binding Message}" />

        <ProgressBar
            IsIndeterminate="{Binding IsIndeterminate}"
            Maximum="{Binding Total}"
            Minimum="0"
            Value="{Binding Value}" />

        <ui:TextBlock
            FontTypography="Body"
            Foreground="{DynamicResource SystemFillColorCriticalBrush}"
            HorizontalAlignment="Center"
            Margin="0,25,0,0"
            Text="{extensions:Loc ProcessingOptimizationDialog.Warning.DontCloseAppOrRestart}" />
    </StackPanel>
</UserControl>