<Page
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Margin="0,32,0,0"
    Title="SettingsPage"
    d:DataContext="{d:DesignInstance pages:SettingsPage}"
    d:DesignHeight="1200"
    d:DesignWidth="800"
    mc:Ignorable="d"
    ui:Design.Background="Black"
    ui:Design.Foreground="White"
    x:Class="optimizerDuck.UI.Views.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:converters="clr-namespace:optimizerDuck.Common.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ext="clr-namespace:optimizerDuck.Common.Extensions"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pages="clr-namespace:optimizerDuck.UI.Views.Pages"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Page.Resources>
        <converters:ThemeToIndexConverter x:Key="ThemeToIndexConverter" />
    </Page.Resources>

    <StackPanel>
        <ui:TextBlock
            FontTypography="Title"
            Text="{ext:Loc Settings.Header.Title}"
            TextWrapping="Wrap" />

        <StackPanel Margin="0,20,0,0">
            <ui:TextBlock
                FontTypography="BodyStrong"
                Text="{ext:Loc Settings.Appearance.Header}"
                TextWrapping="Wrap" />
            <ui:CardControl Icon="{ui:SymbolIcon Globe24}">
                <ui:CardControl.Header>
                    <StackPanel>
                        <ui:TextBlock
                            FontTypography="BodyStrong"
                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                            Margin="0"
                            Text="{ext:Loc Settings.Appearance.LanguageSelector.Title}"
                            TextWrapping="Wrap" />
                        <ui:TextBlock
                            Appearance="Secondary"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Text="{ext:Loc Settings.Appearance.LanguageSelector.Description}"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </ui:CardControl.Header>
                <ComboBox
                    DisplayMemberPath="DisplayName"
                    ItemsSource="{Binding ViewModel.Languages}"
                    SelectedValue="{Binding ViewModel.SelectedCultureName, Mode=TwoWay}"
                    SelectedValuePath="Culture.Name" />
            </ui:CardControl>
            <ui:CardControl Icon="{ui:SymbolIcon Color24}">
                <ui:CardControl.Header>
                    <StackPanel>
                        <ui:TextBlock
                            Appearance="Primary"
                            FontTypography="BodyStrong"
                            Margin="0"
                            Text="{ext:Loc Settings.Appearance.ThemeSelector.Title}"
                            TextWrapping="Wrap" />
                        <ui:TextBlock
                            Appearance="Secondary"
                            Text="{ext:Loc Settings.Appearance.ThemeSelector.Description}"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </ui:CardControl.Header>
                <ComboBox SelectedIndex="{Binding ViewModel.CurrentApplicationTheme, Converter={StaticResource ThemeToIndexConverter}, Mode=TwoWay}">
                    <ComboBoxItem Content="{ext:Loc Settings.Appearance.ThemeSelector.Light}" />
                    <ComboBoxItem Content="{ext:Loc Settings.Appearance.ThemeSelector.Dark}" />
                    <ComboBoxItem Content="{ext:Loc Settings.Appearance.ThemeSelector.HighContrast}" />
                </ComboBox>
            </ui:CardControl>

            <StackPanel Margin="0,20,0,0">
                <ui:TextBlock
                    FontTypography="BodyStrong"
                    Text="{ext:Loc Settings.Optimize.Header}"
                    TextWrapping="Wrap" />

                <ui:CardControl>
                    <ui:CardControl.Icon>
                        <ui:SymbolIcon Foreground="{DynamicResource SystemFillColorCriticalBrush}" Symbol="Clock24" />
                    </ui:CardControl.Icon>
                    <ui:CardControl.Header>
                        <StackPanel>
                            <ui:TextBlock
                                Appearance="Primary"
                                FontTypography="BodyStrong"
                                Margin="0"
                                Text="{ext:Loc Settings.Optimize.ShellTimeout.Title}"
                                TextWrapping="Wrap" />
                            <ui:TextBlock
                                Appearance="Secondary"
                                Text="{ext:Loc Settings.Optimize.ShellTimeout.Description}"
                                TextWrapping="Wrap" />
                            <ui:TextBlock
                                Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                                Text="{ext:Loc Settings.Optimize.ShellTimeout.Warning}"
                                TextWrapping="Wrap" />
                        </StackPanel>
                    </ui:CardControl.Header>

                    <ui:NumberBox
                        Minimum="1000"
                        SmallChange="1000"
                        Value="{Binding ViewModel.ShellTimeoutMs, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </ui:CardControl>
            </StackPanel>

            <StackPanel Margin="0,20,0,0">
                <ui:TextBlock
                    FontTypography="BodyStrong"
                    Text="{ext:Loc Settings.Bloatware.Header}"
                    TextWrapping="Wrap" />

                <ui:CardControl Icon="{ui:SymbolIcon UninstallApp24}">
                    <ui:CardControl.Header>
                        <StackPanel>
                            <ui:TextBlock
                                Appearance="Primary"
                                FontTypography="BodyStrong"
                                Margin="0"
                                Text="{ext:Loc Settings.Bloatware.RemoveProvisioned.Title}"
                                TextWrapping="Wrap" />
                            <ui:TextBlock
                                Appearance="Secondary"
                                Text="{ext:Loc Settings.Bloatware.RemoveProvisioned.Description}"
                                TextWrapping="Wrap" />
                        </StackPanel>
                    </ui:CardControl.Header>

                    <ui:ToggleSwitch Command="{Binding ViewModel.ToggleRemoveProvisionedCommand}" IsChecked="{Binding ViewModel.RemoveProvisioned, Mode=TwoWay}" />
                </ui:CardControl>
            </StackPanel>

            <StackPanel Margin="0,20,0,0">
                <ui:TextBlock
                    FontTypography="BodyStrong"
                    Text="{ext:Loc Settings.Advanced.Header}"
                    TextWrapping="Wrap" />

                <ui:CardAction Command="{Binding ViewModel.ClearResourcesCommand}">
                    <ui:CardAction.Icon>
                        <ui:SymbolIcon Foreground="{DynamicResource SystemFillColorCriticalBrush}" Symbol="Archive24" />
                    </ui:CardAction.Icon>
                    <StackPanel>
                        <ui:TextBlock
                            Appearance="Primary"
                            FontTypography="BodyStrong"
                            Margin="0"
                            Text="{ext:Loc Settings.Advanced.ClearResources.Title}"
                            TextWrapping="Wrap" />
                        <ui:TextBlock
                            Appearance="Secondary"
                            Text="{ext:Loc Settings.Advanced.ClearResources.Description}"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </ui:CardAction>

                <ui:CardAction Command="{Binding ViewModel.ClearAllRevertDataCommand}">
                    <ui:CardAction.Icon>
                        <ui:SymbolIcon Foreground="{DynamicResource SystemFillColorCriticalBrush}" Symbol="DatabaseLightning20" />
                    </ui:CardAction.Icon>
                    <StackPanel>
                        <ui:TextBlock
                            Appearance="Primary"
                            FontTypography="BodyStrong"
                            Margin="0"
                            Text="{ext:Loc Settings.Advanced.ClearRevertDatas.Title}"
                            TextWrapping="Wrap" />
                        <ui:TextBlock
                            Appearance="Secondary"
                            Text="{ext:Loc Settings.Advanced.ClearRevertDatas.Description}"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </ui:CardAction>

                <ui:CardAction Command="{Binding ViewModel.OpenRootDirCommand}" Icon="{ui:SymbolIcon Folder24}">
                    <StackPanel>
                        <ui:TextBlock
                            Appearance="Primary"
                            FontTypography="BodyStrong"
                            Margin="0"
                            Text="{ext:Loc Settings.Advanced.OpenRootDir.Title}"
                            TextWrapping="Wrap" />
                        <ui:TextBlock
                            Appearance="Secondary"
                            Text="{ext:Loc Settings.Advanced.OpenRootDir.Description}"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </ui:CardAction>
            </StackPanel>

            <StackPanel Margin="0,20,0,0">
                <ui:TextBlock
                    FontTypography="BodyStrong"
                    Text="{ext:Loc Settings.About.Header}"
                    TextWrapping="Wrap" />

                <ui:CardAction Command="{Binding ViewModel.OpenWebsiteCommand}" CommandParameter="Web">
                    <ui:CardAction.Icon>
                        <ui:ImageIcon Height="25" Source="pack://application:,,,/optimizerDuck;component/Resources/Images/Duck.png" />
                    </ui:CardAction.Icon>

                    <StackPanel>
                        <ui:TextBlock
                            Appearance="Primary"
                            FontTypography="BodyStrong"
                            Text="optimizerDuck"
                            TextWrapping="Wrap" />
                        <ui:TextBlock
                            Appearance="Secondary"
                            Text="{ext:Loc Settings.About.Duck.Description,
                                           {Binding ViewModel.Version}}"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </ui:CardAction>

                <ui:CardAction
                    Command="{Binding ViewModel.OpenWebsiteCommand}"
                    CommandParameter="Help"
                    Icon="{ui:SymbolIcon ChatBubblesQuestion24}">
                    <StackPanel>
                        <ui:TextBlock
                            FontTypography="BodyStrong"
                            Padding="0"
                            Text="{ext:Loc Settings.About.NeedHelp.Title}" />
                        <ui:TextBlock
                            Appearance="Secondary"
                            Text="{ext:Loc Settings.About.NeedHelp.Description}"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </ui:CardAction>

                <ui:CardAction Command="{Binding ViewModel.OpenAcknowledgementsCommand}" VerticalAlignment="Stretch">
                    <ui:CardAction.Icon>
                        <ui:SymbolIcon Symbol="DocumentHeart24" />
                    </ui:CardAction.Icon>
                    <StackPanel>
                        <ui:TextBlock
                            Appearance="Primary"
                            FontTypography="BodyStrong"
                            Text="{ext:Loc Settings.About.Acknowledgements.Title}"
                            TextWrapping="Wrap" />
                        <ui:TextBlock
                            Appearance="Secondary"
                            Text="{ext:Loc Settings.About.Acknowledgements.Description}"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </ui:CardAction>

                <ui:TextBlock
                    FontTypography="Subtitle"
                    Margin="0,15,0,0"
                    Text="{ext:Loc Settings.About.LearnMore}"
                    TextTrimming="WordEllipsis"
                    TextWrapping="Wrap" />
                <StackPanel Margin="7,0,0,0">
                    <ui:HyperlinkButton Content="{ext:Loc Settings.About.LearnMore.Website}" NavigateUri="{Binding ViewModel.Website}" />
                    <ui:HyperlinkButton Content="{ext:Loc Settings.About.LearnMore.Contribute}" NavigateUri="{Binding ViewModel.Contribute}" />
                    <ui:HyperlinkButton Content="{ext:Loc Settings.About.LearnMore.Documentation}" NavigateUri="{Binding ViewModel.Documentation}" />
                    <ui:HyperlinkButton Content="{ext:Loc Settings.About.LearnMore.Community}" NavigateUri="{Binding ViewModel.Community}" />
                    <ui:HyperlinkButton Content="{ext:Loc Settings.About.LearnMore.Acknowledgements}" NavigateUri="{Binding ViewModel.Acknowledgements}" />
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</Page>