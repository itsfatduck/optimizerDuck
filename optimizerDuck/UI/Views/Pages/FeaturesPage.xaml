<Page
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Margin="0,32,0,0"
    ScrollViewer.CanContentScroll="False"
    Title="Features"
    d:DataContext="{d:DesignInstance vm:FeaturesViewModel}"
    d:DesignHeight="650"
    d:DesignWidth="800"
    mc:Ignorable="d"
    ui:Design.Background="Black"
    ui:Design.Foreground="White"
    x:Class="optimizerDuck.UI.Views.Pages.FeaturesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:converters="clr-namespace:optimizerDuck.Common.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ext="clr-namespace:optimizerDuck.Common.Extensions"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:optimizerDuck.UI.ViewModels.Pages"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />
        <converters:WidthToColumnConverter x:Key="WidthToColumnConverter" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="42,0,42,16">
            <ui:TextBlock
                FontTypography="Title"
                Margin="0,0,0,8"
                Text="{ext:Loc ToggleFeatures.Title}" />
            <ui:TextBlock
                Appearance="Secondary"
                Text="{ext:Loc ToggleFeatures.Description}"
                TextWrapping="Wrap" />
        </StackPanel>

        <ui:VirtualizingItemsControl
            Grid.Row="1"
            ItemsSource="{Binding ViewModel.Categories}"
            Margin="42,0,42,0">
            <ui:VirtualizingItemsControl.Template>
                <ControlTemplate TargetType="ItemsControl">
                    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                        <ItemsPresenter />
                    </ScrollViewer>
                </ControlTemplate>
            </ui:VirtualizingItemsControl.Template>

            <ui:VirtualizingItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ScrollViewer}, Converter={StaticResource WidthToColumnConverter}}" VerticalAlignment="Top" />
                </ItemsPanelTemplate>
            </ui:VirtualizingItemsControl.ItemsPanel>

            <ui:VirtualizingItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Margin" Value="5" />
                    <Setter Property="HorizontalAlignment" Value="Stretch" />
                </Style>
            </ui:VirtualizingItemsControl.ItemContainerStyle>

            <ui:VirtualizingItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:FeatureCategoryItemViewModel}">
                    <ui:CardAction
                        Command="{Binding DataContext.ViewModel.NavigateToCategoryCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                        CommandParameter="{Binding}"
                        IsChevronVisible="False"
                        Padding="20,16">
                        <Grid RowDefinitions="Auto,Auto,Auto">
                            <ui:SymbolIcon
                                FontSize="32"
                                Grid.Row="0"
                                HorizontalAlignment="Left"
                                Margin="0,0,0,12"
                                Symbol="{Binding Icon}" />

                            <ui:TextBlock
                                FontTypography="Subtitle"
                                Grid.Row="1"
                                Margin="0,0,0,6"
                                Text="{Binding Name}"
                                TextWrapping="Wrap" />

                            <ui:TextBlock
                                Appearance="Secondary"
                                FontTypography="Body"
                                Grid.Row="2"
                                Text="{Binding Description}"
                                TextWrapping="Wrap" />
                        </Grid>
                    </ui:CardAction>
                </DataTemplate>
            </ui:VirtualizingItemsControl.ItemTemplate>
        </ui:VirtualizingItemsControl>

        <Border Grid.Row="1" Visibility="{Binding ViewModel.IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ui:ProgressRing
                    Height="48"
                    HorizontalAlignment="Center"
                    IsIndeterminate="True"
                    Margin="0,0,0,16"
                    VerticalAlignment="Center"
                    Width="48" />
                <ui:TextBlock
                    FontTypography="BodyStrong"
                    HorizontalAlignment="Center"
                    Text="{ext:Loc ToggleFeatures.Loading}" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
