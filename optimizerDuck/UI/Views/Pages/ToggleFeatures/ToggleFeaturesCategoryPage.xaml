<Page
    x:Class="optimizerDuck.UI.Views.Pages.ToggleFeatures.ToggleFeaturesCategoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:optimizerDuck.Common.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ext="clr-namespace:optimizerDuck.Common.Extensions"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:optimizerDuck.UI.ViewModels.Pages"
    Title="{Binding ViewModel.CategoryName}"
    Margin="0,32,0,0"
    d:DataContext="{d:DesignInstance vm:ToggleFeatureCategoryViewModel}"
    d:DesignHeight="650"
    d:DesignWidth="800"
    ui:Design.Background="Black"
    ui:Design.Foreground="White"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ScrollViewer.CanContentScroll="False"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="42,0,42,16">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <ui:SymbolIcon Symbol="{Binding ViewModel.CategoryIcon}" FontSize="24" Margin="0,0,12,0" VerticalAlignment="Center" />
                <ui:TextBlock FontTypography="Title" Text="{Binding ViewModel.CategoryName}" VerticalAlignment="Center" />
            </StackPanel>
            <ui:TextBlock Appearance="Secondary" Text="{Binding ViewModel.CategoryDescription}" TextWrapping="Wrap" />
        </StackPanel>

        <WrapPanel Grid.Row="1" Margin="42,0,42,16" Orientation="Horizontal">
            <ui:TextBox
                MinWidth="300"
                Margin="0,0,8,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Icon="{ui:SymbolIcon Search24}"
                PlaceholderText="{ext:Loc Common.Search.Placeholder}"
                Text="{Binding ViewModel.SearchText, UpdateSourceTrigger=PropertyChanged}" />

            <ComboBox
                VerticalAlignment="Center"
                SelectedIndex="{Binding ViewModel.SelectedSortByIndex}">
                <ComboBoxItem Content="{ext:Loc Common.SortBy.Name}" />
            </ComboBox>
        </WrapPanel>

        <ScrollViewer
            Grid.Row="2"
            HorizontalScrollBarVisibility="Disabled"
            Margin="42,0,42,0"
            VerticalScrollBarVisibility="Auto">

            <!--  Feature Cards  -->
            <ItemsControl ItemsSource="{Binding ViewModel.Features}" Margin="0,0,0,16">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ui:CardControl Margin="0,0,0,8" Visibility="{Binding IsVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                            <ui:CardControl.Header>
                                <StackPanel VerticalAlignment="Center">
                                    <ui:TextBlock
                                        FontTypography="Body"
                                        Text="{Binding Name}"
                                        TextWrapping="Wrap" />
                                    <ui:TextBlock
                                        Margin="0,4,0,0"
                                        Appearance="Secondary"
                                        FontTypography="Caption"
                                        Text="{Binding Description}"
                                        TextTrimming="WordEllipsis"
                                        TextWrapping="Wrap" />
                                </StackPanel>
                            </ui:CardControl.Header>
                            <ui:ToggleSwitch
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Command="{Binding ToggleCommand}"
                                IsChecked="{Binding IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </ui:CardControl>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Border
            Grid.Row="2"
            Visibility="{Binding ViewModel.IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ui:ProgressRing
                    Width="48"
                    Height="48"
                    Margin="0,0,0,16"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    IsIndeterminate="True" />
                <ui:TextBlock
                    HorizontalAlignment="Center"
                    FontTypography="BodyStrong"
                    Text="{ext:Loc ToggleFeatures.Loading}" />
            </StackPanel>
        </Border>
    </Grid>
</Page>