<Page
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Margin="0,32,0,0"
    ScrollViewer.CanContentScroll="False"
    Title="StartupManagerPage"
    d:DataContext="{d:DesignInstance local:StartupManagerPage}"
    d:DesignHeight="1200"
    d:DesignWidth="800"
    mc:Ignorable="d"
    ui:Design.Background="Black"
    ui:Design.Foreground="White"
    x:Class="optimizerDuck.UI.Views.Pages.StartupManagerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:converters="clr-namespace:optimizerDuck.Common.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ext="clr-namespace:optimizerDuck.Common.Extensions"
    xmlns:local="clr-namespace:optimizerDuck.UI.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:optimizerDuck.Core.Models.Optimization.StartupManager"
    xmlns:startupManager="clr-namespace:optimizerDuck.Core.Models.Optimization.StartupManager"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:GreaterThanZeroToVisibilityConverter x:Key="GreaterThanZeroToVisibilityConverter" />
        <converters:EmptyWhileNotLoadingConverter x:Key="EmptyWhileNotLoadingConverter" />
    </Page.Resources>

    <Grid RowDefinitions="Auto, *">
        <!--  Header  -->
        <StackPanel Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <ui:TextBlock FontTypography="Title" Text="{ext:Loc StartupManager.Header.Title}" />
                    <ui:TextBlock
                        Appearance="Secondary"
                        FontTypography="Body"
                        Margin="0,4,0,0"
                        Text="{ext:Loc StartupManager.Header.Description}"
                        TextWrapping="Wrap" />
                </StackPanel>
                <WrapPanel
                    Grid.Column="1"
                    Orientation="Horizontal"
                    VerticalAlignment="Center">
                    <ui:Button
                        Appearance="Primary"
                        Command="{Binding ViewModel.RefreshCommand}"
                        Content="{ext:Loc StartupManager.Menu.Refresh}"
                        Icon="{ui:SymbolIcon ArrowClockwise24,
                                             FontSize=18}" />
                </WrapPanel>
            </Grid>
        </StackPanel>

        <!--  Loading Indicator  -->
        <Border Grid.Row="1" Visibility="{Binding ViewModel.IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ui:ProgressRing
                    Height="48"
                    HorizontalAlignment="Center"
                    IsIndeterminate="True"
                    Margin="0,0,0,16"
                    VerticalAlignment="Center"
                    Width="48" />
                <ui:TextBlock
                    FontTypography="BodyStrong"
                    HorizontalAlignment="Center"
                    Text="{ext:Loc StartupManager.Loading}" />
                <ui:TextBlock
                    Appearance="Secondary"
                    FontTypography="Caption"
                    HorizontalAlignment="Center"
                    Margin="0,4,0,0"
                    Text="{ext:Loc StartupManager.Loading.Hint}" />
            </StackPanel>
        </Border>

        <!--  Empty State (no data at all)  -->
        <Border Grid.Row="1">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding ViewModel.HasData}" Value="False" />
                                <Condition Binding="{Binding ViewModel.IsLoading}" Value="False" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible" />
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ui:SymbolIcon
                    FontSize="46"
                    Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,15"
                    Symbol="AppGeneric24" />
                <ui:TextBlock
                    FontTypography="Subtitle"
                    HorizontalAlignment="Center"
                    Text="{ext:Loc StartupManager.Empty.Title}" />
                <ui:TextBlock
                    Appearance="Secondary"
                    FontTypography="Body"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,10"
                    Text="{ext:Loc StartupManager.Empty.Description}" />
                <ui:Button
                    Appearance="Primary"
                    Command="{Binding ViewModel.RefreshCommand}"
                    Content="{ext:Loc StartupManager.Menu.Refresh}"
                    HorizontalAlignment="Center"
                    Icon="{ui:SymbolIcon ArrowClockwise24,
                                         FontSize=18}" />
            </StackPanel>
        </Border>

        <!--  Main Content  -->
        <Border Grid.Row="1" Visibility="{Binding ViewModel.HasData, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ScrollViewer Margin="0,0,16,0">
                <StackPanel>
                    <!--  ═══════════ Startup Apps Section ═══════════  -->
                    <ui:CardExpander
                        Icon="{ui:SymbolIcon Apps24}"
                        IsExpanded="True"
                        Margin="0,0,0,12"
                        Visibility="{Binding ViewModel.HasApps, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <ui:CardExpander.Header>
                            <StackPanel Orientation="Horizontal">
                                <ui:TextBlock
                                    FontTypography="BodyStrong"
                                    Text="{ext:Loc StartupManager.Apps.Title}"
                                    VerticalAlignment="Center" />
                                <Border
                                    Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                                    CornerRadius="10"
                                    Margin="8,0,0,0"
                                    Padding="8,2"
                                    VerticalAlignment="Center">
                                    <ui:TextBlock
                                        Appearance="Tertiary"
                                        FontTypography="Caption"
                                        Text="{Binding ViewModel.Apps.Count}"
                                        VerticalAlignment="Center" />
                                </Border>
                            </StackPanel>
                        </ui:CardExpander.Header>

                        <StackPanel>
                            <!--  Apps Toolbar  -->
                            <WrapPanel Margin="0,0,0,8" Orientation="Horizontal">
                                <ui:TextBox
                                    Icon="{ui:SymbolIcon Search24}"
                                    Margin="0,0,8,0"
                                    MinWidth="200"
                                    PlaceholderText="{ext:Loc Common.Search.Placeholder}"
                                    Text="{Binding ViewModel.AppSearchText, UpdateSourceTrigger=PropertyChanged}"
                                    VerticalAlignment="Center" />
                                <ComboBox
                                    Margin="0,0,8,0"
                                    SelectedIndex="{Binding ViewModel.AppSortByIndex}"
                                    ToolTip="{ext:Loc Common.SortBy}"
                                    VerticalAlignment="Center">
                                    <ComboBoxItem HorizontalContentAlignment="Left">
                                        <Grid ColumnDefinitions="Auto Auto" HorizontalAlignment="Left">
                                            <ui:SymbolIcon
                                                HorizontalAlignment="Left"
                                                Margin="0,0,6,0"
                                                Symbol="TextSortAscending24" />
                                            <TextBlock
                                                Grid.Column="1"
                                                HorizontalAlignment="Left"
                                                Text="{ext:Loc Common.SortBy.Name}" />
                                        </Grid>
                                    </ComboBoxItem>
                                    <ComboBoxItem HorizontalContentAlignment="Left">
                                        <Grid ColumnDefinitions="Auto Auto" HorizontalAlignment="Left">
                                            <ui:SymbolIcon
                                                HorizontalAlignment="Left"
                                                Margin="0,0,6,0"
                                                Symbol="ToggleRight24" />
                                            <TextBlock
                                                Grid.Column="1"
                                                HorizontalAlignment="Left"
                                                Text="{ext:Loc Common.SortBy.Status}" />
                                        </Grid>
                                    </ComboBoxItem>
                                    <ComboBoxItem HorizontalContentAlignment="Left">
                                        <Grid ColumnDefinitions="Auto Auto" HorizontalAlignment="Left">
                                            <ui:SymbolIcon
                                                HorizontalAlignment="Left"
                                                Margin="0,0,6,0"
                                                Symbol="Folder24" />
                                            <TextBlock
                                                Grid.Column="1"
                                                HorizontalAlignment="Left"
                                                Text="{ext:Loc Common.SortBy.Path}" />
                                        </Grid>
                                    </ComboBoxItem>
                                </ComboBox>
                                <ui:Button
                                    Appearance="Secondary"
                                    Command="{Binding ViewModel.OpenStartupSettingsCommand}"
                                    Icon="{ui:SymbolIcon Settings24,
                                                         FontSize=16}"
                                    Padding="8"
                                    ToolTip="{ext:Loc StartupManager.Menu.OpenSettings}"
                                    VerticalAlignment="Center" />
                            </WrapPanel>

                            <!--  No search results (Apps)  -->
                            <StackPanel
                                HorizontalAlignment="Center"
                                Margin="0,32,0,32"
                                VerticalAlignment="Center">
                                <StackPanel.Visibility>
                                    <MultiBinding Converter="{StaticResource EmptyWhileNotLoadingConverter}">
                                        <Binding Path="ViewModel.Apps.Count" />
                                        <Binding Path="ViewModel.IsLoading" />
                                    </MultiBinding>
                                </StackPanel.Visibility>
                                <ui:SymbolIcon
                                    FontSize="36"
                                    Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                                    HorizontalAlignment="Center"
                                    Margin="0,0,0,10"
                                    Symbol="SearchInfo24" />
                                <ui:TextBlock
                                    Appearance="Secondary"
                                    FontTypography="Body"
                                    HorizontalAlignment="Center"
                                    Text="{ext:Loc Bloatware.Search.NoResults}" />
                            </StackPanel>

                            <!--  Apps List  -->
                            <ItemsControl ItemsSource="{Binding ViewModel.Apps}" Visibility="{Binding ViewModel.Apps.Count, Converter={StaticResource GreaterThanZeroToVisibilityConverter}}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate DataType="{x:Type startupManager:StartupApp}">
                                        <Grid>
                                            <ui:CardAction
                                                Command="{Binding DataContext.ViewModel.OpenLocationCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                CommandParameter="{Binding}"
                                                IsChevronVisible="False"
                                                Margin="0,0,0,4"
                                                Padding="12,10"
                                                x:Name="ActionCard">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <Image
                                                        Grid.Column="0"
                                                        Height="36"
                                                        Margin="0,0,12,0"
                                                        Source="{Binding LogoImage}"
                                                        Stretch="UniformToFill"
                                                        VerticalAlignment="Center"
                                                        Width="36" />
                                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                        <DockPanel LastChildFill="False">
                                                            <ui:TextBlock FontTypography="BodyStrong" Text="{Binding Name}" />
                                                            <Border
                                                                Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                                                                CornerRadius="4"
                                                                Margin="8,0,0,0"
                                                                Padding="6,1"
                                                                VerticalAlignment="Center">
                                                                <ui:TextBlock
                                                                    Appearance="Tertiary"
                                                                    FontTypography="Caption"
                                                                    Text="{Binding LocationDisplay}"
                                                                    VerticalAlignment="Center" />
                                                            </Border>
                                                        </DockPanel>
                                                        <ui:TextBlock
                                                            Appearance="Secondary"
                                                            FontTypography="Caption"
                                                            Margin="0,2,0,0"
                                                            Text="{Binding Publisher}" />
                                                        <ui:TextBlock
                                                            Appearance="Disabled"
                                                            FontTypography="Caption"
                                                            Margin="0,2,0,0"
                                                            Text="{Binding Command}"
                                                            TextTrimming="CharacterEllipsis" />
                                                    </StackPanel>
                                                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                                                        <ui:ToggleSwitch
                                                            Cursor="Hand"
                                                            IsChecked="{Binding IsEnabled, Mode=TwoWay}"
                                                            Margin="0,0,16,0"
                                                            OffContent="{ext:Loc Common.Toggle.Off}"
                                                            OnContent="{ext:Loc Common.Toggle.On}"
                                                            VerticalAlignment="Center" />
                                                        <ui:SymbolIcon FontSize="16" Symbol="Open24" />
                                                    </StackPanel>
                                                </Grid>
                                            </ui:CardAction>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ui:CardExpander>

                    <!--  ═══════════ Scheduled Tasks Section ═══════════  -->
                    <ui:CardExpander
                        Icon="{ui:SymbolIcon CalculatorArrowClockwise24}"
                        IsExpanded="True"
                        Margin="0,0,0,12"
                        Visibility="{Binding ViewModel.HasTasks, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <ui:CardExpander.Header>
                            <StackPanel Orientation="Horizontal">
                                <ui:TextBlock
                                    FontTypography="BodyStrong"
                                    Text="{ext:Loc StartupManager.Tasks.Title}"
                                    VerticalAlignment="Center" />
                                <Border
                                    Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                                    CornerRadius="10"
                                    Margin="8,0,0,0"
                                    Padding="8,2"
                                    VerticalAlignment="Center">
                                    <ui:TextBlock
                                        Appearance="Tertiary"
                                        FontTypography="Caption"
                                        Text="{Binding ViewModel.Tasks.Count}"
                                        VerticalAlignment="Center" />
                                </Border>
                            </StackPanel>
                        </ui:CardExpander.Header>

                        <StackPanel>
                            <!--  Tasks Toolbar  -->
                            <WrapPanel Margin="0,0,0,8" Orientation="Horizontal">
                                <ui:TextBox
                                    Icon="{ui:SymbolIcon Search24}"
                                    Margin="0,0,8,0"
                                    MinWidth="200"
                                    PlaceholderText="{ext:Loc Common.Search.Placeholder}"
                                    Text="{Binding ViewModel.TaskSearchText, UpdateSourceTrigger=PropertyChanged}"
                                    VerticalAlignment="Center" />
                                <ComboBox
                                    Margin="0,0,8,0"
                                    SelectedIndex="{Binding ViewModel.TaskSortByIndex}"
                                    ToolTip="{ext:Loc Common.SortBy}"
                                    VerticalAlignment="Center">
                                    <ComboBoxItem HorizontalContentAlignment="Left">
                                        <Grid ColumnDefinitions="Auto Auto" HorizontalAlignment="Left">
                                            <ui:SymbolIcon
                                                HorizontalAlignment="Left"
                                                Margin="0,0,6,0"
                                                Symbol="TextSortAscending24" />
                                            <TextBlock
                                                Grid.Column="1"
                                                HorizontalAlignment="Left"
                                                Text="{ext:Loc Common.SortBy.Name}" />
                                        </Grid>
                                    </ComboBoxItem>
                                    <ComboBoxItem HorizontalContentAlignment="Left">
                                        <Grid ColumnDefinitions="Auto Auto" HorizontalAlignment="Left">
                                            <ui:SymbolIcon
                                                HorizontalAlignment="Left"
                                                Margin="0,0,6,0"
                                                Symbol="ToggleRight24" />
                                            <TextBlock
                                                Grid.Column="1"
                                                HorizontalAlignment="Left"
                                                Text="{ext:Loc Common.SortBy.Status}" />
                                        </Grid>
                                    </ComboBoxItem>
                                    <ComboBoxItem HorizontalContentAlignment="Left">
                                        <Grid ColumnDefinitions="Auto Auto" HorizontalAlignment="Left">
                                            <ui:SymbolIcon
                                                HorizontalAlignment="Left"
                                                Margin="0,0,6,0"
                                                Symbol="Folder24" />
                                            <TextBlock
                                                Grid.Column="1"
                                                HorizontalAlignment="Left"
                                                Text="{ext:Loc Common.SortBy.Path}" />
                                        </Grid>
                                    </ComboBoxItem>
                                </ComboBox>
                                <ui:ToggleSwitch
                                    Cursor="Hand"
                                    IsChecked="{Binding ViewModel.HideMicrosoftTasks, Mode=TwoWay}"
                                    Margin="0,0,8,0"
                                    OffContent="{ext:Loc StartupManager.Tasks.HideMicrosoft}"
                                    OnContent="{ext:Loc StartupManager.Tasks.HideMicrosoft}"
                                    VerticalAlignment="Center" />
                                <ui:Button
                                    Appearance="Secondary"
                                    Command="{Binding ViewModel.OpenTaskSchedulerCommand}"
                                    Icon="{ui:SymbolIcon Settings24,
                                                         FontSize=16}"
                                    Padding="8"
                                    ToolTip="{ext:Loc ScheduledTasks.Menu.OpenSettings}"
                                    VerticalAlignment="Center" />
                            </WrapPanel>

                            <!--  No search results (Tasks)  -->
                            <StackPanel
                                HorizontalAlignment="Center"
                                Margin="0,32,0,32"
                                VerticalAlignment="Center">
                                <StackPanel.Visibility>
                                    <MultiBinding Converter="{StaticResource EmptyWhileNotLoadingConverter}">
                                        <Binding Path="ViewModel.Tasks.Count" />
                                        <Binding Path="ViewModel.IsLoading" />
                                    </MultiBinding>
                                </StackPanel.Visibility>
                                <ui:SymbolIcon
                                    FontSize="36"
                                    Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                                    HorizontalAlignment="Center"
                                    Margin="0,0,0,10"
                                    Symbol="SearchInfo24" />
                                <ui:TextBlock
                                    Appearance="Secondary"
                                    FontTypography="Body"
                                    HorizontalAlignment="Center"
                                    Text="{ext:Loc Bloatware.Search.NoResults}" />
                            </StackPanel>

                            <!--  Tasks List  -->
                            <ItemsControl ItemsSource="{Binding ViewModel.Tasks}" Visibility="{Binding ViewModel.Tasks.Count, Converter={StaticResource GreaterThanZeroToVisibilityConverter}}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate DataType="{x:Type startupManager:StartupTask}">
                                        <Border
                                            Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                                            CornerRadius="6"
                                            Margin="0,0,0,6"
                                            Padding="16,12">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>

                                                <Image
                                                    Grid.Column="0"
                                                    Height="36"
                                                    Margin="0,0,12,0"
                                                    Source="{Binding LogoImage}"
                                                    Stretch="UniformToFill"
                                                    VerticalAlignment="Center"
                                                    Width="36" />

                                                <StackPanel
                                                    Grid.Column="1"
                                                    Margin="0,0,12,0"
                                                    VerticalAlignment="Center">
                                                    <ui:TextBlock FontTypography="BodyStrong" Text="{Binding TaskName}" />
                                                    <ui:TextBlock
                                                        Appearance="Secondary"
                                                        FontTypography="Caption"
                                                        Margin="0,4,0,0"
                                                        Text="{Binding Description}"
                                                        TextWrapping="Wrap" />
                                                    <ui:TextBlock
                                                        Appearance="Disabled"
                                                        FontTypography="Caption"
                                                        Margin="0,2,0,0"
                                                        Text="{Binding TaskPath}" />
                                                    <ItemsControl ItemsSource="{Binding TriggerTypes}" Margin="0,4,0,0">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <WrapPanel Orientation="Horizontal" />
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border
                                                                    Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                                                                    CornerRadius="4"
                                                                    Margin="0,0,4,2"
                                                                    Padding="6,2">
                                                                    <StackPanel Orientation="Horizontal">
                                                                        <ui:SymbolIcon
                                                                            FontSize="15"
                                                                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                                            Margin="0,0,4,0"
                                                                            Symbol="Timer24" />
                                                                        <ui:TextBlock
                                                                            FontTypography="Caption"
                                                                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                                            Text="{Binding}"
                                                                            VerticalAlignment="Center" />
                                                                    </StackPanel>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                    <ui:TextBlock
                                                        Appearance="Disabled"
                                                        FontTypography="Caption"
                                                        Margin="0,2,0,0"
                                                        Text="{Binding ActionSummary}"
                                                        TextTrimming="CharacterEllipsis" />
                                                </StackPanel>

                                                <StackPanel
                                                    Grid.Column="2"
                                                    Orientation="Horizontal"
                                                    VerticalAlignment="Center">
                                                    <ui:Button
                                                        Appearance="Transparent"
                                                        Command="{Binding DataContext.ViewModel.ViewTaskDetailsCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                        CommandParameter="{Binding}"
                                                        Icon="{ui:SymbolIcon Info24,
                                                                             FontSize=16}"
                                                        Margin="0,0,4,0"
                                                        Padding="8"
                                                        ToolTip="{ext:Loc ScheduledTasks.Action.Details}" />
                                                    <ui:ToggleSwitch
                                                        Cursor="Hand"
                                                        IsChecked="{Binding IsEnabled, Mode=TwoWay}"
                                                        OffContent="{ext:Loc Common.Toggle.Off}"
                                                        OnContent="{ext:Loc Common.Toggle.On}"
                                                        VerticalAlignment="Center" />
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ui:CardExpander>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>